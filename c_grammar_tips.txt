    1. нужно учитывать, как выставляются конструкции {}
т.е. для тесновложенных они могут не выставляться, для множества операций должны выставляться
    2. проверить, можно ли объявлять переменные через double
    3. учитывать, что объявление переменных может происходить в цикле for

Разбираемся с:
    Статические массивы:
    1. Пусть объявление размера будет обязательным
        (ошибку нужно будет выдавать либо после текстуальной подстановки, если в ней решим подставлять значения переменных)
        (или в процессе обработки, если у нас будет таблица значений переменных)
    2. Пусть пока задаётся только тип элементов массива int/double и его размер либо в явном виде,
        либо через переменную.
    
Отложим:
Объявление и инициализация
    - список переменных в виде double i, j, k;
    - инициализация переменных в списке типа double i = 1, j, k;
    - двойная инициализация в виде int i = j = k = 6;
    - инициализация массива в явном виде (для стат)
    - инициализация массива в явном виде (для дин) (не помню, возможно ли это)
Работа с массивами
    - функции для работы с динамическими массивами (calloc malloc resize)
    - инициализация массивов из файла, в цикле и т.д.
Работа с входными данными
    - наложить ограничения на использования argv, argc (поскольку при стат анализе
    неизвестны значения)
Циклы for
    - добавление конструкций ++i с учётом эффектов
Сложное
    - обработка функций
